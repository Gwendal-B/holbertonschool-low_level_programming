/**
 * _isalpha - verifie si un caractere est en minuscule ou maj
 * @c: le caractere a tester (int)
 *
 * Return: 1 si minuscule ou maj, 0 sinon
 */
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
