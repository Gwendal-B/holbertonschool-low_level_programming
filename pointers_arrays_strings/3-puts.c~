#include "main.h"

/**
 *_puts - affiche une chaine de caractere suivi d'un passage a la ligne
 *@str: pointeur vers la chaine a afficher
 *
 *Return: R
 */
void _puts(char *str)
{
	int a = 0;

	while (str[a] !=0)
	{
		_putchar(str[a]);
		a++;
	}
	_putchar('\n');
}
